resources:
  pipelines:
    bronze_silver_pipeline:
      name: medallion_breweries_bronze_silver
      
      catalog: ${var.catalog}
      schema: ${var.schema}

      configuration:
        breweries.catalog: ${var.catalog}
        breweries.schema: ${var.schema}
      
      serverless: true
      continuous: false
      photon: true

      libraries:
      - file:
          path: ../src/medallion_dlt_breweries_bronze_silver/transformations/01bronze_breweries.py
